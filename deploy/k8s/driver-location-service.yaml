apiVersion: v1
kind: Service
metadata:
  name: driver-location-service
  labels:
    app: driver-location-service
spec:
  selector:
    app: driver-location-service
  ports:
    - name: http
      port: 8082
      targetPort: http
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: driver-location-service
  labels:
    app: driver-location-service
spec:
  replicas: 2
  revisionHistoryLimit: 3
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxUnavailable: 0
      maxSurge: 1
  selector:
    matchLabels:
      app: driver-location-service
  template:
    metadata:
      labels:
        app: driver-location-service
      annotations:
        prometheus.io/scrape: "true"
        prometheus.io/path: /actuator/prometheus
        prometheus.io/port: "8082"
    spec:
      terminationGracePeriodSeconds: 30
      containers:
        - name: driver-location-service
          image: dispatchgrid/driver-location-service:dev
          imagePullPolicy: IfNotPresent
          ports:
            - name: http
              containerPort: 8082
          env:
            - name: PORT
              value: "8082"
            - name: SHARD_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: dispatchgrid-shard-credentials
                  key: SHARD_PASSWORD
          envFrom:
            - configMapRef:
                name: dispatchgrid-env
          startupProbe:
            httpGet:
              path: /actuator/health/liveness
              port: http
            periodSeconds: 5
            failureThreshold: 36
          readinessProbe:
            httpGet:
              path: /actuator/health/readiness
              port: http
            periodSeconds: 5
            timeoutSeconds: 4
            failureThreshold: 3
          livenessProbe:
            httpGet:
              path: /actuator/health/liveness
              port: http
            periodSeconds: 10
            timeoutSeconds: 4
            failureThreshold: 3
          lifecycle:
            preStop:
              exec:
                command: ["sh", "-c", "sleep 5"]
          resources:
            requests:
              cpu: 250m
              memory: 384Mi
            limits:
              memory: 768Mi
